#!/bin/bash
# ref: http://qiita.com/koara-local/items/940ce66e2ecd8e4d8582

[ $# -ne 1 ] && exit 1
need_version=$1
current_version=$(tmux -V | awk '{print $2}')

[[ $(echo "$current_version > $need_version" | bc) != 0 ]]
