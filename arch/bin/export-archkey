#!/bin/bash -e

exp() {
    target=$1
    base=$target/key
    sudo mkdir -p /target/key
    sudo cp -R ~/.ssh $base/ssh
    sudo cp -R ~/.gnupg $base/gpg
}

if [ $# -eq 1 ]; then
    exp $*
else
    echo "requires target directory" >&2
fi

